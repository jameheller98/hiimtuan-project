spring.application.name=api-gateway
server.port=8765
spring.config.import=optional:configserver:http://localhost:8888
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka

spring.datasource.url=jdbc:mysql://localhost:3306/hiimtuan?serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD:Daddassawsawsd9*}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# Environment dynamic
spring.profiles.active=development
spring.cloud.config.profile=development

# Grpc
spring.grpc.client.channels.mail-service.address=0.0.0.0:9090
spring.grpc.client.channels.user-service.address=0.0.0.0:9091

# Actuator
management.endpoints.web.exposure.include=metrics,health,info

# App
app.frontend-url=${FRONTEND_URL:http://localhost:3000}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}